# Server Configuration
PORT=5000
NODE_ENV=development

# API Keys (REQUIRED)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# PDF Processing Configuration
PDF_CACHE_TTL_MINUTES=30
PDF_CACHE_MAX_ENTRIES=100
PDF_PARALLEL_BATCHES=2

# Database (REQUIRED for premium features - uses in-memory storage if not set)
# Example: postgresql://user:password@localhost:5432/modmapper
DATABASE_URL=postgresql://user:password@localhost:5432/modmapper

# CORS Configuration (Production)
# Comma-separated list of allowed origins
# Example: https://yourdomain.com,https://app.yourdomain.com
# ALLOWED_ORIGINS=

# ============================================================================
# Authentication & Session Management (REQUIRED for premium features)
# ============================================================================

# Session secret - Generate with: openssl rand -base64 64
SESSION_SECRET=your_session_secret_here_generate_with_openssl_rand

# Magic link secret - Generate with: openssl rand -base64 64
MAGIC_LINK_SECRET=your_magic_link_secret_here_generate_with_openssl_rand

# Application URL (for email links and redirects)
APP_URL=http://localhost:5000

# ============================================================================
# Email Configuration (REQUIRED for user authentication)
# ============================================================================

# SMTP Configuration (Example using SendGrid)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_api_key_here

# Email sender address
FROM_EMAIL=noreply@modmapper.com

# ============================================================================
# Stripe Integration (REQUIRED for Pro subscriptions)
# ============================================================================

# Stripe API keys (get from: https://dashboard.stripe.com/apikeys)
# Use test keys for development: sk_test_...
# Use live keys for production: sk_live_...
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe webhook secret (get from: https://dashboard.stripe.com/webhooks)
# Used to verify webhook signatures
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Pro subscription price ID (get from: https://dashboard.stripe.com/products)
# Create a product called "ModMapper Pro" with monthly pricing
STRIPE_PRO_PRICE_ID=price_your_price_id_here
